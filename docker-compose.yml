services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  client:
    build: ./client
    restart: unless-stopped
    expose:
      - "3000"

  server:
    build: ./server
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      client:
        condition: service_started
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      ACCESS_TOKEN_SECRET: ${ACCESS_TOKEN_SECRET}
      REFRESH_TOKEN_SECRET: ${REFRESH_TOKEN_SECRET}
      PORT: "4000"
      NODE_ENV: production
      NEXT_DEV_URL: http://client:3000
      CLIENT_ORIGIN: ${PUBLIC_URL}
      COOKIE_SECURE: ${COOKIE_SECURE}
      TRUST_PROXY: ${TRUST_PROXY:-false}
    expose:
      - "4000"

  # Media server: nhận RTMP từ OBS (1935) → phát HLS (8888) cho web
  mediamtx:
    image: bluenviron/mediamtx:latest
    restart: unless-stopped
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
    ports:
      - "1935:1935"       # OBS bắn RTMP vào cổng này
      - "8189:8189/udp"   # WebRTC media (ICE/UDP) — trình duyệt kết nối trực tiếp
    expose:
      - "8888"            # HLS nội bộ, Caddy proxy ra HTTPS
      - "8889"            # WebRTC/WHEP signaling nội bộ, Caddy proxy ra HTTPS

  # Reverse proxy + auto HTTPS. Nếu KHÔNG dùng domain, có thể bỏ service này
  # và mở lại "ports: 80:4000" ở service server.
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - server
      - mediamtx
    ports:
      - "80:80"
      - "443:443"
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pgdata:
  caddy_data:
  caddy_config:
