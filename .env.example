# ─── Copy file này thành .env trên VPS rồi điền giá trị thật ───
# (KHÔNG commit .env lên git)

# Database
POSTGRES_DB=voidprotocol
POSTGRES_USER=void_user
POSTGRES_PASSWORD=doi_mat_khau_db_manh_o_day

# JWT secrets — sinh chuỗi ngẫu nhiên dài. Gợi ý: openssl rand -base64 48
ACCESS_TOKEN_SECRET=thay_bang_chuoi_ngau_nhien_dai_1
REFRESH_TOKEN_SECRET=thay_bang_chuoi_ngau_nhien_dai_2

# ─── Chạy với DOMAIN + HTTPS (khuyên dùng) ───
SITE_DOMAIN=an1307.vn
PUBLIC_URL=https://an1307.vn
COOKIE_SECURE=true
TRUST_PROXY=true

# ─── Hoặc chạy bằng IP/HTTP (không Caddy) ───
# Bỏ service caddy, mở lại "ports: 80:4000" ở server, rồi:
# PUBLIC_URL=http://162.4.176.46
# COOKIE_SECURE=false
# TRUST_PROXY=false
